const initialState = {
    name: 'Dmitriy',
    lastName: 'Lomov',
};

export const profileReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}