import React from "react";
// import { useRouteMatch } from "react-router-dom";

const Settings = (props) => {
    // console.log(useRouteMatch('/settings').url);
    return <div>Страница настроек</div>;
};

export default Settings;
